package org.group17.Logging;

/**
 * Test class to test the Logging System.
 * 
 * @author Ludwig
 *
 */
public class TestLog {

	/**
	 * This tests if the constructor creates a new logging file.
	 */
	/*
	 * @Test public void testLog() { int iAmountOfFilesBefore = new
	 * File("Logging/").listFiles().length; Log lLog = new Log(); int
	 * iAmountOfFilesAfter = new File("Logging/").listFiles().length;
	 * assertEquals(iAmountOfFilesBefore + 1, iAmountOfFilesAfter); }
	 */
}